services:
  - type: web
    name: genieacs-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_CONNECTION_URL
        sync: false
    autoDeploy: true
    plan: free
